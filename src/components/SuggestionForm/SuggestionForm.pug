div.item__suggestion
    each i in suggestionsFilms
        div.item__internal
            img.item__suggestion__image(src= i.src alt= i.alt)
            div.item__suggestion__title
                a(href= "#")= i.title
            div.item__suggestion__status
                div= i.status
